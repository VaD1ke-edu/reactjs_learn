export function commentsReducer(state = [], action) {
    switch (action.type) {

    }
    return state;
}

export function currentCommentReducer(state = {}, action) {
    switch (action.type) {

    }
    return state;
}